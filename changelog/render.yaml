services:
   - type: web
     name: changelog
     env: node
     buildCommand: npm install && npm run build
     startCommand: npm run start
     plan: standard
     branch: master
     region: frankfurt
     envVars:
      - key: NODE_VERSION
        value: 16.14