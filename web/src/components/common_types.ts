export enum Size {
    XS = "xs",
    SM = "sm",
    MD = "md",
    LG = "lg",
    XL = "XL"
}

export enum Variant {
    FLAT = 'flat',
    GRADIENT = 'gradient',
    INVERTED = 'inverted',
    LIGHT = 'light'
}

export enum Color {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    DEFAULT = 'default',
    SUCCESS = 'success',
    WARNING = 'warning',
    DANGER = 'danger'
}